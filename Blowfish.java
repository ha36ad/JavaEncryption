import javax.crypto.Cipher;
import javax.crypto.spec.SecretKeySpec;
import java.util.Base64;
import java.util.Scanner;

}
public static String blowfish (String password); {
     //Ask user for a message + key
    public static void main(String[] args) {
    InputKey = new Scanner(System.in);
    System.out.println("Enter your key");
    String KeyInput = KeyInput.nextLine()
    String key;
    InputMessage = new Scanner(System.in);
    System.out.println("Enter message");
    String MessageInput = MessageInput.nextLine()
    String message;
        
    }
    byte [] KeyData = (key).getBytes();
    SecretKeySpec SK = new SecretKeySpec (MessageData, "Blowfish");
    Cipher cipher = Cipher.getInstance ("Blowfish");
    cipher.int(Cipher.ENCRYPT_MODE, securekey);
    
   
    //Encrypting the message
    byte [] encrypted = cipher.doFinal (message.getBytes());
        
        
}
