import javax.crypto.Cipher;
import javax.crypto.spec.SecretKeySpec;
public static String blowfish (String password); {
    try {
    byte [] keyData = (key).getBytes();
    SecretKeySpec secretKeySpec = new SecretKeySpec (keyData, s:"Blowfish");
    }
}
